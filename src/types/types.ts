// types/types.ts

export interface ThemeUiConfig {
  colors: {
    primary: string;
    secondary: string;
  };
  fonts: {
    body: string;
    heading: string;
  };
  // Add other fields as defined in your schema
}

// Add more types/interfaces as needed for your project
